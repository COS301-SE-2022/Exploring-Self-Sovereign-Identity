name: ExploringSelfSovereignIdentity

on:
  push:
    branches: 
      - '**'
  pull_request:
    branches:
      - '**'

jobs:
  API:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Setup .NET
      uses: actions/setup-dotnet@v2
      with:
        dotnet-version: 6.0.x
    - name: Restore dependencies
      working-directory: ./ExploringSelfSovereignIdentityAPI
      run: dotnet restore
    - name: BuildAPI
      working-directory: ./ExploringSelfSovereignIdentityAPI
      run: dotnet build --no-restore
    - name: TestAPI
      working-directory: ./ExploringSelfSovereignIdentityAPIUnitTests
      run: dotnet test
  
  Frontend:
  
    runs-on: ubuntu-latest
  
    steps:
      - uses: actions/checkout@master
      - uses: actions/setup-node@master
      - name: Installing project dependencies
        working-directory: ./SSIVueJsClient
        run: npm install
      - name: Building the project
        working-directory: ./SSIVueJsClient
        run: npm run build
      - name: Running Tests
        working-directory: ./SSIVueJsClient
        run: npm run test:unit
  
  CodeCov:
  
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@master
    - uses: codecov/codecov-action@v2
      with:
       token: ${{ secrets.CODECOV_TOKEN }} # not required for public repos
       files: ./SSIVueJsClient/node_modules/.bin/codecov ./ExploringSelfSovereignIdentityAPIUnitTests/coverage.opencover.xml # optional
       flags: unittests # optional
       name: codecov-umbrella # optional
       fail_ci_if_error: true # optional (default = false)
       verbose: true # optional (default = false)
